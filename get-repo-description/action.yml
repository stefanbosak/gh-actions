name: 'Get Repository Description'
description: 'Fetches the GitHub repository description via the GitHub API'

inputs:
  github-token:
    description: 'GitHub token for API authentication'
    required: true
  repository:
    description: 'Repository in owner/name format'
    required: false
    default: ${{ github.repository }}

outputs:
  description:
    description: 'The repository description'
    value: ${{ steps.fetch.outputs.description }}

runs:
  using: composite
  steps:
    - name: 'Fetch repository description'
      id: fetch
      shell: bash
      run: |
        DESCRIPTION=$(curl -s \
          -H "Authorization: Bearer ${{ inputs.github-token }}" \
          -H "Accept: application/vnd.github+json" \
          "https://api.github.com/repos/${{ inputs.repository }}" \
          | jq -r '.description')
        echo "description=$DESCRIPTION" >> "${GITHUB_OUTPUT}"
